1. **Analyze Failure**: The test `.github/workflows/test.yml` checks for `dist/eahandbookcompiler.epub` after running `uv run python -m eahandbookcompiler convert dist/sample.md --output-dir dist`.
2. **Examine `main.py`**: The `convert` command generates output files based on the stem of the input file. So `dist/sample.md` generates `dist/sample.epub` and `dist/sample.pdf`.
3. **The bug**: In `.github/workflows/test.yml`, the script checks for `dist/eahandbookcompiler.epub`, which does not match the actual behavior of the codebase. My changes to `scraper.py` did not cause this bug; this test workflow script seems to have been bugged or outdated from before. Wait, did I introduce a change somewhere? No, I only touched `scraper.py`. Let's fix the workflow file `.github/workflows/test.yml` to check for `dist/sample.epub` instead. Or, more simply, change the output verification to check for `dist/sample.epub` and `dist/sample.pdf`.
